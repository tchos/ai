<!-- Bar de navigation -->
<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand" href="{{ path('homepage') }}">COSEFADI !</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" 
        aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor01">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link taille" href="{{ path('homepage') }}">
                    <i class="fas fa-home"></i>
                    Accueil <span class="sr-only">(current)</span>
                </a>
            </li>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" 
                    aria-haspopup="true" aria-expanded="false">
                    <i class="fas fa-walking"></i>
                    Pensions de réversion
                </a>
                <div class="dropdown-menu" style="">
                    <a class="dropdown-item" href="{{ path('reversion_index') }}">
                        <i class="fas fa-laptop"></i>
                        Saisir pension de réversion
                    </a>
                    <a class="dropdown-item" href="#">
                        <i class="fas fa-eye"></i>
                        Voir mes saisies
                    </a>
                </div>
            </li>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" 
                    role="button" 
                    aria-haspopup="true" aria-expanded="false">
                    <i class="fas fa-wheelchair"></i>
                    Pensions d'invalidité
                </a>
                <div class="dropdown-menu" style="">
                    <a class="dropdown-item" href="{{ path('invalidite_index') }}">
                        <i class="fas fa-laptop"></i>
                        Saisir pension d'invalidité
                    </a>
                    <a class="dropdown-item" href="#">
                        <i class="fas fa-eye"></i>
                        Voir mes saisies
                    </a>
                </div>
            </li>
            <li class="nav-item">
                <a class="nav-link taille" href="#">
                    <i class="fas fa-chart-bar"></i>
                    Statistiques de saisie
                </a>
            </li>
        </ul>
        <ul class="navbar-nav ml-auto">
            {% if app.user %}
                <li class="nav-item dropdown">
                    <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" id="accountDropDownLink">
                        <i class="fas fa-user-check"></i>
                        {{ app.user.fullName }}
                    </a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropDownLink">
                        <div class="dropdown-divider"></div>
                        <a href="{{ path('account_password') }}" class="dropdown-item">
                            <i class="fas fa-user-lock"></i>
                            Modifier mot de passe
                        </a>
                        <a href="{{ path('account_profile') }}" class="dropdown-item">
                            <i class="fas fa-user-edit"></i>
                            Modifier mon profil
                        </a>
                        <div class="dropdown-divider"></div>
                        <a href="{{ path('account_logout') }}" class="dropdown-item"
                            id="myLinkToConfirm" data-confirm-title="Attention !" 
                            data-confirm-message="Etes-vous sûr de vouloir vous déconnectez ?">
                            <i class="fas fa-sign-out-alt"></i>
                            Déconnexion
                        </a>
                    </div>
                </li>
            {% else %}
                <li class="nav-item active">
                    <a class="nav-link taille" href="#">
                        <i class="fas fa-sign-in-alt"></i>
                        Connexion <span class="sr-only">(current)</span>
                    </a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>   

{# Alertes #}
{% for label, messages in app.flashes %}
    <div class="container">
        <div class="alert alert-{{ label }}">
            {% for message in messages %}
                <p>{{ message | raw }}</p>
            {% endfor %}
        </div>
    </div>
{% endfor %}

{% block javascripts %}
    <script>
        $('#myLinkToConfirm').confirmModal();
    </script>
{% endblock %}