<!-- Bar de navigation -->
<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand text-primary" href="{{ path('admin_index') }}">DASHBOARD !</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" 
        aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor01">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active dropdown">
                <a class="nav-link dropdown-toggle text-dark" data-toggle="dropdown" href="#" role="button" 
                    aria-haspopup="true" aria-expanded="false">
                    <i class="fas fa-users-cog"></i>
                    Pensions de réversion
                </a>
                <div class="dropdown-menu" style="">
                    <a class="dropdown-item" href="{{ path('admin_reversion') }}">
                        <i class="fas fa-user-alt-slash"></i>
                        Saisir pension de réversion
                    </a>
                    <a class="dropdown-item" href="{{ path('admin_reversion_show') }}">
                        <i class="fas fa-eye"></i>
                        Voir mes saisies
                    </a>
                </div>
            </li>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle text-dark" data-toggle="dropdown" href="#" role="button" 
                    aria-haspopup="true" aria-expanded="false">
                    <i class="fas fa-users-cog"></i>
                    Pensions d'invalidité
                </a>
                <div class="dropdown-menu" style="">
                    <a class="dropdown-item" href="{{ path('admin_invalidite') }}">
                        <i class="fas fa-user-alt-slash"></i>
                        Saisir pension d'invalidité
                    </a>
                    <a class="dropdown-item" href="{{ path('admin_invalidite_show') }}">
                        <i class="fas fa-eye"></i>
                        Voir mes saisies
                    </a>
                </div>
            </li>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle text-dark" data-toggle="dropdown" href="#" role="button" 
                    aria-haspopup="true" aria-expanded="false">
                    <i class="fas fa-users-cog"></i>
                    Gestion des users
                </a>
                <div class="dropdown-menu" style="">
                    <a class="dropdown-item" href="{{ path('admin_account_register') }}">
                        <i class="fas fa-user-plus"></i>
                        Ajouter un user
                    </a>
                    <a class="dropdown-item" href="{{ path('admin_user_index') }}">
                        <i class="fas fa-eye"></i>
                        Lister les users
                    </a>
                </div>
            </li>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle text-dark" data-toggle="dropdown" href="#" role="button" 
                    aria-haspopup="true" aria-expanded="false">
                    <i class="fas fa-people-carry"></i>
                    Gestion des équipes
                </a>
                <div class="dropdown-menu" style="">
                    <a class="dropdown-item" href="{{ path('admin_equipe_create') }}">
                        <i class="fas fa-user-plus"></i>
                        Ajouter une équipe
                    </a>
                    <a class="dropdown-item" href="{{ path('admin_equipe_show') }}">
                        <i class="fas fa-eye"></i>
                        Lister les équipes
                    </a>
                </div>
            </li>
        </ul>
        <ul class="navbar-nav ml-auto">
            {% if app.user %}
                <li class="nav-item dropdown">
                    <a href="#" class="nav-link dropdown-toggle text-primary" data-toggle="dropdown" 
                        id="accountDropDownLink">
                        <i class="fas fa-user-check"></i> {{app.user.fullName}}
                    </a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropDownLink">
                        <div class="dropdown-divider"></div>
                        <a href="{{ path('admin_password_update') }}" class="dropdown-item">
                            <i class="fas fa-user-lock"></i>
                            Modifier mot de passe
                        </a>
                        <a href="{{ path('admin_account_profile') }}" class="dropdown-item">
                            <i class="fas fa-user-edit"></i>
                            Modifier mon profil
                        </a>
                        <div class="dropdown-divider"></div>
                        <a href="{{ path('admin_account_logout') }}" class="dropdown-item"
                            id="myLinkToConfirm" data-confirm-title="Attention !" 
                            data-confirm-message="Etes-vous sûr de vouloir vous déconnectez ?">
                            <i class="fas fa-sign-out-alt"></i>
                            Déconnexion
                        </a>
                    </div>
                </li>
            {% else %}
                <li class="nav-item active">
                    <a class="nav-link taille" href="{{ path('admin_account_register') }}">
                        <i class="fas fa-user-plus"></i>
                        Inscription <span class="sr-only">(current)</span>
                    </a>
                </li>
                <li class="nav-item active">
                    <a class="nav-link taille" href="{{ path('account_login') }}">
                        <i class="fas fa-sign-in-alt"></i>
                        Connexion <span class="sr-only">(current)</span>
                    </a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>   

{# Alertes #}
{% for label, messages in app.flashes %}
    <div class="container">
        <div class="alert alert-{{ label }}">
            {% for message in messages %}
                <p>{{ message | raw }}</p>
            {% endfor %}
        </div>
    </div>
{% endfor %}

{% block javascripts %}
    <script>
        $('#myLinkToConfirm').confirmModal();
    </script>
{% endblock %}